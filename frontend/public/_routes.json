{
  "version": 1,
  "description": "Route API and public invite/scheduling requests to Workers, everything else to Pages",
  "include": [
    "/api/*",
    "/auth/*",
    "/admin/*",
    "/i/*",
    "/g/*",
    "/open/*",
    "/ra/*",
    "/health",
    "/test/*"
  ],
  "exclude": []
}
